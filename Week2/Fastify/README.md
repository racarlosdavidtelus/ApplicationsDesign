## COMMANDS
```sh
# Install with npm:
npm i fastify --save

# Install with yarn:
yarn add fastify

# Run your server from CLI
npm i fastify-cli

# Then, add the following lines to package.json:
{
  "scripts": {
    "start": "fastify start server.js"
  }
}

# run the server
npm start

```
